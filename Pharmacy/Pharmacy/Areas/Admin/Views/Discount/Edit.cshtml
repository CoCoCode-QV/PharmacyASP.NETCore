@model Pharmacy.Data.Discount

@{
    ViewData["Title"] = "Cập nhật mã giảm giá";
    Layout = "~/Areas/Admin/Views/_Layout_Admin.cshtml";
}

<h2>Cập nhật mã giảm giá</h2>

<hr />
@using (Html.BeginForm("Edit", "Discount", FormMethod.Post))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">

        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.DiscountId)

        <div class="form-group row">
            @Html.LabelFor(model => model.DiscountName, htmlAttributes: new { @class = "col-form-label col-sm-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.DiscountName, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.DiscountName, "", new { @class = "text-danger" })
            </div>
        </div>
        <div class="form-group row">
            @Html.LabelFor(model => model.DiscountPercent, htmlAttributes: new { @class = "col-form-label col-sm-2" })
            <div class="col-sm-10">
                @Html.EditorFor(model => model.DiscountPercent, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.DiscountPercent, "", new { @class = "text-danger" })
            </div>
        </div>
        <div class="form-group row">
            @Html.LabelFor(model => model.DiscountStartDate, htmlAttributes: new { @class = "col-form-label col-sm-2" })
            <div class="col-sm-10">
                @{
                    string Startdate = Model.DiscountStartDate?.ToString("yyyy-MM-ddTHH:mm:ss");
                    <input type="datetime-local" name="DiscountStartDate" value="@Startdate" />
                    @Html.ValidationMessageFor(model => model.DiscountStartDate, "", new { @class = "text-danger" })
                }
            </div>
        </div>
        <div class="form-group row">
            @Html.LabelFor(model => model.DiscountEndDate, htmlAttributes: new { @class = "col-form-label col-sm-2" })
            <div class="col-sm-10">
                @{
                    string Enddate = Model.DiscountEndDate?.ToString("yyyy-MM-ddTHH:mm:ss");
                    <input type="datetime-local" name="DiscountEndDate" value="@Enddate" />
                    @Html.ValidationMessageFor(model => model.DiscountEndDate, "", new { @class = "text-danger" })
                }
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Lưu" class="btn btn-default" />
            </div>
        </div>
    </div>
}
<div>
    @Html.ActionLink("Quay lại", "Index")
</div>


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
